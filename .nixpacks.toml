[phases.setup]
nixPkgs = ['php82', 'php82Extensions.mysqli', 'apache2']

[phases.install]
cmds = [
  "mkdir -p /var/log/apache2",
  "mkdir -p /var/run/apache2"
]

[start]
cmd = "apache2 -f /app/apache2.conf -DFOREGROUND"