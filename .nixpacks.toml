[phases.setup]
nixPkgs = ['nginx', 'php82', 'php82Extensions.mysqli']

[phases.install]
cmds = [
  "mkdir -p /var/log/nginx",
  "mkdir -p /var/cache/nginx"
]

[phases.build]
cmds = "chmod -R 777 /app"

[start]
cmd = "nginx -c /app/nginx.conf"